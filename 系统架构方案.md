# 问鼎铭校 — 小升初 AI 智能伴学系统 · 系统架构方案

> 版本：v1.0（待确认）  
> 日期：2026-09-18  
> 设计约束：**2000–3000 注册用户、课堂并发约几十人、轻量单体、可维护、避免过度工程**

---

## 一、设计目标与核心约束

### 1.1 设计目标

为「问鼎铭校」小升初 AI 智能伴学系统搭建一个**可支撑 2000–3000 注册用户、200 以内峰值并发**的全栈应用，首期聚焦「学习闭环」：

```
老师布置任务 → 学生答题 → 客观题自动批改 → 掌握度更新 → 错题本记录 → 老师看板反馈
```

### 1.2 核心约束

| 约束 | 说明 |
|---|---|
| 用户规模 | 2000–3000 注册用户；同时在线课堂 40 人左右，峰值并发 < 200 |
| 单体优先 | 不拆分微服务，降低部署和运维复杂度 |
| 可维护 | 代码分层清晰（控制器/服务/仓储），配置集中，类型安全 |
| 不重复造轮子 | 登录、支付、推送、视频等外围能力用第三方；核心数据与算法自研 |
| 合规 | 义务教育阶段学科类培训、未成年人信息采集需满足最小必要、监护人同意、审计留痕 |

---

## 二、总体架构

### 2.1 架构概览

采用经典 **前后端分离 + 单体后端 + 单数据库** 架构：

```
┌─────────────────────────────────────────────────────────────┐
│                          客户端                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  学生端 H5   │  │  教师端 PC   │  │  管理端 PC   │      │
│  │   Vue3 SPA   │  │   Vue3 SPA   │  │   Vue3 SPA   │      │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘      │
└─────────┼─────────────────┼─────────────────┼──────────────┘
          │                 │                 │
          └─────────────────┼─────────────────┘
                            │ HTTPS / JSON
┌───────────────────────────▼──────────────────────────────┐
│                     Nginx 反向代理                        │
│              静态文件服务 / 负载均衡 / HTTPS               │
└───────────────────────────┬──────────────────────────────┘
                            │
┌───────────────────────────▼──────────────────────────────┐
│                  NestJS 后端应用                          │
│  控制器 → 服务层 → 仓储层 → TypeORM → PostgreSQL         │
│  认证 / 业务逻辑 / 掌握度计算 / SSE 实时看板              │
└───────────────────────────┬──────────────────────────────┘
                            │
┌───────────────────────────▼──────────────────────────────┐
│                 PostgreSQL 数据库                         │
│   用户 / 班级 / 题库 / 任务 / 答题记录 / 掌握度 / 错题本   │
└───────────────────────────────────────────────────────────┘
```

### 2.2 架构决策说明

| 决策 | 选择 | 理由 |
|---|---|---|
| 服务形态 | 前后端分离单体应用 | 2000–3000 用户无需微服务；前后端分离便于未来移动端/小程序扩展 |
| 后端框架 | Node.js + NestJS | TypeScript 类型安全、依赖注入清晰、生态成熟、开发快；足够支撑目标规模 |
| 前端框架 | Vue3 + Vite | 组件化、响应式、与现有 HTML/CSS 原型迁移成本低 |
| 数据库 | PostgreSQL | 开源、稳定、支持 JSONB 与数组，关系型模型契合题库/任务/答题记录 |
| 缓存 | 不使用 Redis | 规模小，PostgreSQL 连接池 + 应用内存缓存足够；需要时再加 |
| 实时通信 | SSE（Server-Sent Events） | 课堂 40 人场景足够，无需 WebSocket 连接管理；兼容现有 HTTP 基础设施 |
| 文件存储 | 本地磁盘 / 对象存储 | 题目图片、学生拍照先存本地挂载目录，量大后迁移至 OSS/COS |
| AI 能力 | 规则模型 + 通用 LLM API | 自适应推题用可解释规则；AI 讲解调用大模型 API 生成文本 |
| 部署 | 单台云服务器 + Docker Compose | 2–4 核 4–8G 即可；容器化保证环境一致，降低运维门槛 |

---

## 三、职责边界

### 3.1 前端职责

| 端 | 主要职责 |
|---|---|
| 学生端 H5 | 登录、今日任务、智能练习、答题反馈、错题本、掌握度图谱 |
| 教师端 PC | 课堂实时看板（SSE）、布置任务、学生档案、班级报告（极简） |
| 管理端 PC | 用户管理、班级管理、题库导入/查看/编辑 |

前端不处理业务规则，只负责：
- UI 渲染与状态管理
- API 调用与错误映射
- Token 自动刷新与 401 重试
- 加载态、空状态、离线提示

### 3.2 后端职责

后端按「控制器 → 服务 → 仓储」分层：

| 层 | 职责 |
|---|---|
| 控制器（Controller） | 解析 HTTP 请求、调用服务、格式化响应；不写业务逻辑 |
| 服务层（Service） | 承载业务规则与编排；掌握度计算、任务生成、自适应推荐在此实现 |
| 仓储层（Repository） | 负责数据库查询与外部调用；使用 TypeORM Repository |

后端模块划分：

```
auth          登录、JWT、刷新令牌、权限中间件
users         学生/老师/管理员 CRUD、批量导入
classes       班级、班级学生关系
questions     题库 CRUD、导入导出、知识点打标
tasks         任务创建、下发、查询
submissions   学生答题、客观题自动批改、AI 讲解生成
mastery       掌握度计算与查询
wrongbook     错题记录、遗忘曲线推荐
classroom-sse 课堂实时看板事件推送
notifications 站内通知
```

### 3.3 数据库职责

- 存储所有业务数据：用户、班级、题库、任务、答题记录、掌握度、错题本、令牌、审计日志。
- 通过迁移脚本管理 schema 变更，保证可回滚。
- 使用事务保证多步写入一致性（如任务下发、答题批改 + 掌握度更新）。

---

## 四、核心数据模型

### 4.1 实体关系说明

```
users ──1:1── students ──N:1── classes
         │
         └──N:M── class_students

users ──1:1── teachers ──1:N── classes

users ──1:1── admins

knowledge_modules ──1:N── knowledge_points

knowledge_points ──1:N── questions

classes ──1:N── tasks ──1:N── student_tasks ──1:N── submissions

students ──1:N── submissions
questions ──1:N── submissions

students ──1:N── mastery ──N:1── knowledge_points
students ──1:N── wrongbook ──N:1── questions
students ──1:N── notifications
```

### 4.2 表结构设计

```sql
-- 用户表（统一身份，按 role 区分）
CREATE TABLE users (
  id            SERIAL PRIMARY KEY,
  role          VARCHAR(20) NOT NULL CHECK (role IN ('student','teacher','admin')),
  username      VARCHAR(64) NOT NULL UNIQUE,   -- 学号/工号/管理员账号
  password_hash VARCHAR(255) NOT NULL,
  name          VARCHAR(64) NOT NULL,
  phone         VARCHAR(20),
  email         VARCHAR(128),
  status        VARCHAR(20) NOT NULL DEFAULT 'active' CHECK (status IN ('active','inactive')),
  created_at    TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at    TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 学生扩展
CREATE TABLE students (
  user_id       INTEGER PRIMARY KEY REFERENCES users(id) ON DELETE CASCADE,
  student_no    VARCHAR(64) NOT NULL UNIQUE,
  class_id      INTEGER REFERENCES classes(id) ON DELETE SET NULL
);

-- 老师扩展
CREATE TABLE teachers (
  user_id       INTEGER PRIMARY KEY REFERENCES users(id) ON DELETE CASCADE,
  teacher_no    VARCHAR(64) NOT NULL UNIQUE
);

-- 班级
CREATE TABLE classes (
  id            SERIAL PRIMARY KEY,
  name          VARCHAR(64) NOT NULL,           -- 六(3)班
  grade         VARCHAR(32) NOT NULL,           -- 六年级
  teacher_id    INTEGER REFERENCES teachers(user_id),
  created_at    TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 班级学生关系（支持未来一个学生多个班）
CREATE TABLE class_students (
  class_id      INTEGER REFERENCES classes(id) ON DELETE CASCADE,
  student_id    INTEGER REFERENCES students(user_id) ON DELETE CASCADE,
  joined_at     TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  PRIMARY KEY (class_id, student_id)
);

-- 知识点树（模块 → 考点 → 知识点，用 parent_id 自关联）
CREATE TABLE knowledge_points (
  id            SERIAL PRIMARY KEY,
  parent_id     INTEGER REFERENCES knowledge_points(id),
  level         INTEGER NOT NULL CHECK (level IN (1,2,3)),  -- 1模块 2考点 3知识点
  name          VARCHAR(128) NOT NULL,
  code          VARCHAR(64) UNIQUE,            -- 便于导入匹配
  sort_order    INTEGER DEFAULT 0,
  created_at    TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 题库
CREATE TABLE questions (
  id            SERIAL PRIMARY KEY,
  type          VARCHAR(32) NOT NULL CHECK (type IN ('choice','fill','calculation','solution')),
  stem          TEXT NOT NULL,                 -- 题干，支持 LaTeX
  options       JSONB,                         -- 选择题选项 {A:'...',B:'...'}
  answer        TEXT NOT NULL,                 -- 客观题标准答案
  explanation   TEXT,                          -- 解析
  difficulty    INTEGER NOT NULL CHECK (difficulty BETWEEN 1 AND 5),
  knowledge_point_id INTEGER REFERENCES knowledge_points(id),
  error_tags    JSONB,                         -- ['公式代错','单位漏换']
  source        VARCHAR(128),                  -- 校本题库/期中卷
  review_status VARCHAR(20) DEFAULT 'pending' CHECK (review_status IN ('pending','approved','rejected')),
  created_by    INTEGER REFERENCES users(id),
  created_at    TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at    TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 学习任务
CREATE TABLE tasks (
  id            SERIAL PRIMARY KEY,
  title         VARCHAR(255) NOT NULL,
  class_id      INTEGER NOT NULL REFERENCES classes(id) ON DELETE CASCADE,
  created_by    INTEGER NOT NULL REFERENCES users(id),
  question_ids  INTEGER[] NOT NULL,            -- 题目顺序
  settings      JSONB NOT NULL DEFAULT '{}',   -- {difficulty_strategy, time_limit_minutes, due_at}
  status        VARCHAR(20) NOT NULL DEFAULT 'active' CHECK (status IN ('draft','active','closed')),
  created_at    TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 学生任务实例（每个学生一份，记录个人进度）
CREATE TABLE student_tasks (
  id            SERIAL PRIMARY KEY,
  task_id       INTEGER NOT NULL REFERENCES tasks(id) ON DELETE CASCADE,
  student_id    INTEGER NOT NULL REFERENCES students(user_id) ON DELETE CASCADE,
  status        VARCHAR(20) NOT NULL DEFAULT 'assigned' CHECK (status IN ('assigned','in_progress','completed')),
  progress      INTEGER NOT NULL DEFAULT 0 CHECK (progress BETWEEN 0 AND 100),
  score         INTEGER,                       -- 百分制
  started_at    TIMESTAMPTZ,
  completed_at  TIMESTAMPTZ,
  UNIQUE (task_id, student_id)
);

-- 答题记录
CREATE TABLE submissions (
  id                SERIAL PRIMARY KEY,
  student_task_id   INTEGER NOT NULL REFERENCES student_tasks(id) ON DELETE CASCADE,
  student_id        INTEGER NOT NULL REFERENCES students(user_id) ON DELETE CASCADE,
  question_id       INTEGER NOT NULL REFERENCES questions(id),
  answer            TEXT,
  is_correct        BOOLEAN,
  error_tag         VARCHAR(64),               -- 命中错误类型
  ai_explanation    TEXT,                      -- AI 讲解文本
  time_spent_seconds INTEGER DEFAULT 0,
  submitted_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 掌握度（学生对每个知识点）
CREATE TABLE mastery (
  id                  SERIAL PRIMARY KEY,
  student_id          INTEGER NOT NULL REFERENCES students(user_id) ON DELETE CASCADE,
  knowledge_point_id  INTEGER NOT NULL REFERENCES knowledge_points(id) ON DELETE CASCADE,
  score               INTEGER NOT NULL DEFAULT 0 CHECK (score BETWEEN 0 AND 100),
  total_attempts      INTEGER NOT NULL DEFAULT 0,
  correct_attempts    INTEGER NOT NULL DEFAULT 0,
  updated_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  UNIQUE (student_id, knowledge_point_id)
);

-- 错题本
CREATE TABLE wrongbook (
  id              SERIAL PRIMARY KEY,
  student_id      INTEGER NOT NULL REFERENCES students(user_id) ON DELETE CASCADE,
  question_id     INTEGER NOT NULL REFERENCES questions(id),
  first_wrong_at  TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  last_wrong_at   TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  wrong_count     INTEGER NOT NULL DEFAULT 1,
  retry_count     INTEGER NOT NULL DEFAULT 0,
  last_retry_correct BOOLEAN,
  next_review_at  TIMESTAMPTZ,
  status          VARCHAR(20) NOT NULL DEFAULT 'open' CHECK (status IN ('open','mastered','archived')),
  UNIQUE (student_id, question_id)
);

-- 刷新令牌（服务端存储，支持注销）
CREATE TABLE refresh_tokens (
  id            SERIAL PRIMARY KEY,
  user_id       INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  token_hash    VARCHAR(255) NOT NULL UNIQUE,
  expires_at    TIMESTAMPTZ NOT NULL,
  created_at    TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 站内通知
CREATE TABLE notifications (
  id            SERIAL PRIMARY KEY,
  user_id       INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  type          VARCHAR(32) NOT NULL,
  title         VARCHAR(255) NOT NULL,
  content       TEXT,
  read_at       TIMESTAMPTZ,
  created_at    TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 审计日志（合规）
CREATE TABLE audit_logs (
  id            BIGSERIAL PRIMARY KEY,
  user_id       INTEGER REFERENCES users(id),
  action        VARCHAR(64) NOT NULL,
  resource      VARCHAR(64) NOT NULL,
  resource_id   VARCHAR(64),
  ip_address    INET,
  user_agent    TEXT,
  details       JSONB,
  created_at    TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 常用索引
CREATE INDEX idx_questions_kp ON questions(knowledge_point_id);
CREATE INDEX idx_submissions_student ON submissions(student_id);
CREATE INDEX idx_submissions_question ON submissions(question_id);
CREATE INDEX idx_submissions_submitted_at ON submissions(submitted_at);
CREATE INDEX idx_mastery_student ON mastery(student_id);
CREATE INDEX idx_mastery_kp ON mastery(knowledge_point_id);
CREATE INDEX idx_wrongbook_student ON wrongbook(student_id);
CREATE INDEX idx_student_tasks_task ON student_tasks(task_id);
CREATE INDEX idx_tasks_class ON tasks(class_id);
CREATE INDEX idx_audit_logs_created_at ON audit_logs(created_at);
```

---

## 五、前后端接口约定

### 5.1 基础规范

- Base URL：`https://<domain>/api/v1`
- 认证：请求头 `Authorization: Bearer <access_token>`
- 刷新令牌：通过 `HttpOnly` Cookie `refresh_token` 传递
- 内容类型：`Content-Type: application/json`
- 分页请求：`?page=1&page_size=20`（默认 page=1，page_size=20，最大 100）
- 分页响应：

```json
{
  "data": [...],
  "pagination": {
    "page": 1,
    "page_size": 20,
    "total": 156,
    "total_pages": 8
  }
}
```

### 5.2 错误响应格式

```json
{
  "code": "TASK_NOT_FOUND",
  "message": "任务不存在或已删除",
  "details": { "taskId": 123 }
}
```

HTTP 状态码与错误码对应：

| HTTP | 场景 |
|---|---|
| 200 | 成功 |
| 201 | 创建成功 |
| 400 | 请求参数错误 |
| 401 | 未登录或令牌过期 |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 409 | 资源冲突（如重复用户名） |
| 422 | 业务校验失败 |
| 429 | 请求过于频繁 |
| 500 | 服务器内部错误（不暴露堆栈） |

### 5.3 核心接口清单

#### 认证

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/auth/login` | 登录，返回 access_token，设置 refresh_token Cookie |
| POST | `/auth/refresh` | 用 refresh_token 换取新 access_token |
| POST | `/auth/logout` | 注销并吊销 refresh_token |
| GET  | `/auth/me` | 当前登录用户信息 |

#### 用户（管理端）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET    | `/users` | 用户列表（按 role 过滤） |
| POST   | `/users` | 创建用户 |
| PATCH  | `/users/:id` | 修改用户 |
| DELETE | `/users/:id` | 删除用户（写审计日志） |
| POST   | `/users/bulk-import-students` | 批量导入学生（CSV/Excel） |

#### 班级

| 方法 | 路径 | 说明 |
|---|---|---|
| GET    | `/classes` | 班级列表 |
| POST   | `/classes` | 创建班级 |
| GET    | `/classes/:id` | 班级详情 |
| GET    | `/classes/:id/students` | 班级学生列表 |
| POST   | `/classes/:id/students` | 添加学生到班级 |
| DELETE | `/classes/:id/students/:studentId` | 移除学生 |

#### 题库

| 方法 | 路径 | 说明 |
|---|---|---|
| GET    | `/questions` | 题目列表（按知识点、难度、题型、复核状态过滤） |
| POST   | `/questions` | 新增题目 |
| GET    | `/questions/:id` | 题目详情 |
| PATCH  | `/questions/:id` | 编辑题目 |
| POST   | `/questions/import` | 批量导入题目 |
| GET    | `/questions/export` | 导出题目 |

#### 任务

| 方法 | 路径 | 说明 |
|---|---|---|
| GET    | `/tasks` | 老师任务列表 |
| POST   | `/tasks` | 创建任务并下发 |
| GET    | `/tasks/:id` | 任务详情 |
| GET    | `/student/tasks` | 学生任务列表 |
| GET    | `/student/tasks/:id` | 学生任务详情（含题目） |

#### 答题与提交

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/student/submissions` | 提交答案（支持单题/批量） |
| GET  | `/student/submissions/:id` | 提交详情 |
| GET  | `/student/tasks/:id/result` | 学生任务结果统计 |

#### 掌握度

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/student/mastery` | 学生自己的掌握度 |
| GET | `/students/:id/mastery` | 老师查看某学生掌握度 |

#### 错题本

| 方法 | 路径 | 说明 |
|---|---|---|
| GET  | `/student/wrongbook` | 错题列表 |
| POST | `/student/wrongbook/:id/retry` | 错题重做提交 |

#### 教师看板

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/teacher/dashboard` | 班级实时统计 |
| GET | `/teacher/tasks/:id/progress` | 任务进度明细 |
| GET | `/teacher/students/:id/activity` | 学生活动时间线 |
| GET | `/sse/classroom/:classId` | SSE 实时推送课堂事件 |

---

## 六、实时看板 SSE 设计

### 6.1 连接方式

教师端打开课堂看板时，建立 SSE 连接：

```
GET /api/v1/sse/classroom/:classId?taskId=xxx
Authorization: Bearer <token>
```

后端每隔 3–5 秒推送一次聚合事件，减少前端刷新与数据库压力。

### 6.2 事件类型

```json
{
  "event": "classroom.snapshot",
  "data": {
    "classId": 3,
    "taskId": 12,
    "onlineCount": 42,
    "avgProgress": 58,
    "avgCorrectRate": 71,
    "alerts": [
      { "studentId": 2024008, "type": "stuck", "message": "第 5 题停留 4 分钟" }
    ],
    "students": [
      { "studentId": 2024001, "name": "李小明", "progress": 85, "correctRate": 88, "status": "normal" }
    ]
  }
}
```

### 6.3 性能取舍

- 不追求毫秒级实时，3–5 秒聚合刷新对课堂场景足够。
- 不存储实时状态到数据库，使用内存聚合 + 答题记录查询。
- 单课堂 40 人、教师 1 个 SSE 连接，连接数与带宽压力极低。

---

## 七、自适应推题与掌握度计算（第一期规则版）

### 7.1 掌握度更新规则

每个学生对每个知识点维护一个 0–100 的掌握度分数：

```
基础变化量 = 答对 +difficulty_weight * 8，答错 -difficulty_weight * 10
difficulty_weight：难度 1→0.8，2→0.9，3→1.0，4→1.1，5→1.2
时间衰减：超过 7 天的答题记录权重降至 0.7，超过 30 天降至 0.4
最终分数 =  clamp(历史加权平均, 0, 100)
```

同时记录 `total_attempts` / `correct_attempts`，便于老师查看。

### 7.2 自适应推题规则

生成练习时，按以下优先级选 N 题：

1. **知识点选择**：优先选择掌握度 40%–70% 的知识点（提升空间最大）；避开掌握度 >90%（已掌握）和最近 7 天已练习过的知识点。
2. **难度匹配**：
   - 掌握度 < 50%：选难度 1–2 题
   - 50% ≤ 掌握度 < 75%：选难度 3 题
   - ≥ 75%：选难度 4–5 题
3. **去重**：避开该学生最近 7 天已做过的题目。
4. **兜底**：若推荐不足，从当前课程相关知识点中随机补题。

### 7.3 AI 讲解

- 客观题：本地判分，根据 `error_tags` 匹配预设讲解模板；可调用 LLM API 生成更自然的讲解文本。
- 主观题（MVP 不自动批改）：学生拍照上传，老师人工批改；系统可调用 LLM 给老师提供「参考批注」。

---

## 八、安全与合规检查清单

> 本系统涉及义务教育阶段学科培训与未成年人信息，必须按以下清单落地。

### 8.1 基础安全

- [ ] 全站 HTTPS，禁止明文 HTTP 访问。
- [ ] 密码使用 bcrypt 哈希，salt rounds ≥ 10。
- [ ] JWT access_token 有效期 15–30 分钟；refresh_token 有效期 7–30 天，服务端存储并支持吊销。
- [ ] 敏感接口启用 Rate Limiting（登录、注册、导入等）。
- [ ] 所有输入使用 class-validator 校验，拒绝非法参数。
- [ ] 所有数据库操作使用 ORM/参数化查询，防止 SQL 注入。
- [ ] 输出做 HTML 转义，配置 CSP 头，防止 XSS。
- [ ] 错误响应不返回堆栈、SQL、内部路径。
- [ ] CORS 只允许指定前端域名，生产环境禁用通配符。
- [ ] 文件上传限制类型（jpg/png/webp/pdf）和大小（单文件 ≤5MB）。

### 8.2 数据保护与合规

- [ ] 用户协议与隐私政策中明确：收集哪些数据、用途、保存期限、删除方式。
- [ ] 未成年人注册需监护人同意（线上勾选 + 短信/微信确认，留存记录）。
- [ ] 最小必要采集：不收集与教学无关的个人信息（如家庭收入、精确位置）。
- [ ] 支持家长/学生查看、导出、删除个人数据。
- [ ] 不向第三方出售或共享学生数据。
- [ ] 审计日志记录敏感操作：登录、登出、密码修改、用户删除、数据导出、权限变更。
- [ ] 日志中不记录密码、令牌、手机号等敏感字段。
- [ ] 数据库每日自动备份，备份文件加密存储。
- [ ] 上线前咨询当地教育主管部门与法务，确认线上学科培训合规要求。

### 8.3 业务合规

- [ ] 课程宣传不夸大效果，AI 讲解标注「AI 生成，仅供参考」。
- [ ] 提供「讲得不对」反馈入口，人工复核后可修正。
- [ ] 不设置诱导未成年人沉迷的排行榜/连续打卡奖励。
- [ ] 家长可查看学生学习报告，避免向家长推送制造焦虑的内容。

---

## 九、部署方案与性能取舍

### 9.1 服务器规格

| 组件 | 推荐配置 | 说明 |
|---|---|---|
| 云服务器 | 2–4 vCPU / 4–8 GB RAM / 100 GB SSD | 足够支撑 2000–3000 注册用户、200 并发 |
| 操作系统 | Ubuntu 22.04 LTS | 稳定、社区支持好 |
| 容器 | Docker + Docker Compose | 环境一致、部署简单 |
| 反向代理 | Nginx 或 Caddy | Nginx 性能成熟；Caddy 自动 HTTPS 更简单 |
| 数据库 | PostgreSQL 15+ | 与后端同机部署，降低复杂度 |

### 9.2 Docker Compose 部署示意

```yaml
version: '3.8'
services:
  app:
    build: ./backend
    ports:
      - "3000:3000"
    env_file:
      - .env
    depends_on:
      - db
    volumes:
      - ./uploads:/app/uploads
    restart: unless-stopped

  db:
    image: postgres:15-alpine
    env_file:
      - .env
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./backups:/backups
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./frontend/dist:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./cert:/etc/nginx/cert:ro
    depends_on:
      - app
    restart: unless-stopped

volumes:
  pgdata:
```

### 9.3 性能取舍说明

| 方案 | 本期选择 | 理由 |
|---|---|---|
| 缓存 | 不使用 Redis | PostgreSQL 连接池足够；应用层可做短时间内存缓存 |
| 消息队列 | 不使用 | 批量导入/报告生成直接走同步 API，单次 ≤500 题；未来量大再引入 |
| CDN | 不使用 | 静态资源通过 Nginx 提供；用户量增长后上 CDN |
| 数据库读写分离 | 不做 | 单库足以支撑目标规模 |
| 微服务 | 不做 | 单体应用降低开发和运维复杂度 |
| Kubernetes | 不做 | Docker Compose 足够，避免引入编排复杂度 |

### 9.4 备份与监控

- **数据库备份**：每日凌晨 `pg_dump` 导出，上传至对象存储或异地服务器，保留 30 天。
- **应用日志**：结构化 JSON 日志，按天轮转，保留 14 天。
- **监控告警**：
  - 磁盘使用率 > 80% 告警
  - 内存使用率 > 85% 告警
  - 应用 `/health` 连续失败告警
  - 5xx 错误率突增告警（可选接入简单 APM）

---

## 十、项目目录结构

```
D:\小升初系统\
├── backend/                      # NestJS 后端
│   ├── src/
│   │   ├── main.ts
│   │   ├── app.module.ts
│   │   ├── config/               # 配置与 env 校验
│   │   ├── common/               # 异常过滤器、拦截器、守卫、管道
│   │   ├── database/             # migrations / seeds
│   │   └── modules/
│   │       ├── auth/
│   │       ├── users/
│   │       ├── classes/
│   │       ├── questions/
│   │       ├── tasks/
│   │       ├── submissions/
│   │       ├── mastery/
│   │       ├── wrongbook/
│   │       ├── classroom-sse/
│   │       └── notifications/
│   ├── Dockerfile
│   └── package.json
├── frontend/                     # Vue3 前端
│   ├── src/
│   │   ├── main.ts
│   │   ├── App.vue
│   │   ├── api/                  # API 客户端
│   │   ├── stores/               # Pinia 状态
│   │   ├── router/               # 路由
│   │   ├── views/
│   │   │   ├── student/
│   │   │   ├── teacher/
│   │   │   └── admin/
│   │   └── components/
│   ├── Dockerfile
│   └── package.json
├── docker-compose.yml
├── .env.example
├── .gitignore
└── README.md
```

---

## 十一、下一步工作计划

收到你对本架构方案的确认后，立即进入 **Phase 2：技术基础设施 & 核心后端**，按以下顺序分步实施：

1. **初始化项目脚手架**：创建 GitHub 仓库、NestJS 后端、Vue3 前端，配置 ESLint/Prettier。
2. **环境配置与数据库**：`.env.example`、Docker Compose、PostgreSQL、TypeORM 迁移、种子数据。
3. **认证与权限**：登录、JWT/Refresh Token、RBAC 守卫、审计日志。
4. **用户与班级模块**：CRUD、批量导入、班级学生关系。
5. **题库模块**：题目 CRUD、导入导出、知识点树。
6. **任务与答题模块**：任务下发、学生答题、客观题自动批改。
7. **掌握度与错题本**：掌握度更新、错题记录、自适应推荐。
8. **SSE 教师看板**：课堂实时数据聚合与推送。
9. **前端对接**：学生/教师/管理端页面与 API 联调。
10. **集成测试与部署**：冒烟测试、Docker 部署、HTTPS。

每一步完成后，我会说明改动范围、已实现内容和本地自测结果，等你确认后再进入下一步。
